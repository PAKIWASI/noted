local M = {}

function M.setup(opts)
    -- require("notes.config").setup(opts)
    -- require("notes.storage").load()
    -- require("notes.commands").register()


    -- vim.api.nvim_create_autocmd("VimLeavePre", {
    --     group = vim.api.nvim_create_augroup("NotedPluginExit", { clear = true }),
    --     callback = function()
    --         -- require("notes.storage").save()
    --     end,
    -- })
end

return M
